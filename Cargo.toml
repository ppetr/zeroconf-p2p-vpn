[package]
name = "zeroconf-p2p-vpn"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache 2.0"

[dependencies]
tokio-uring = "0.5"
nix = { version = "0.29", features = ["fs", "net", "ioctl"] }
